# Physics Agent 项目优化与问题报告

## 1. 报告目的

本报告基于以下文档综合整理：

- `AGENTS.md`
- `CLAUDE.md`
- `system_prompt.md`
- `PROJECT_ANALYSIS.md`

目标不是重复介绍项目，而是直接回答两个问题：

1. 你这个项目目前最需要优化的地方是什么
2. 如果目标是和顶级智能体产品对标，目前差距在哪里

这份报告适合直接作为后续修改清单使用。

## 2. 当前项目的已有优势

先明确一点，这个项目并不弱，它已经具备几个非常有价值的基础：

- 已经有明确垂直场景，不是空泛通用聊天
- 已经有多模态输入，支持文档和图片
- 已经有实验仿真联动，这是差异化强项
- 已经有教学型角色设定，不是裸模型调用
- 已经有 Web 交互和可演示产品形态
- 已经有 SSE 流式体验和基本 RAG 注入能力

如果只看“原型完成度”，项目已经超过很多只会做聊天框的 demo。

但如果目标是“和顶级智能体相 PK”，问题就不是“能不能用”，而是“够不够强、够不够稳、够不够像真正的 Agent 系统”。

## 3. 从 MD 文档里直接暴露出的问题

## 3.1 文档主线不一致

`AGENTS.md` 和 `CLAUDE.md` 仍然把项目主线写成：

- 后端入口：`app.py`
- 前端逻辑：`static/js/app.js`
- 前端样式：`static/css/style.css`

但你已经确认当前真实主线是：

- 前端入口：`static/index.html`
- 后端主文件：`backend/main.py`
- 当前生效资源：`static/app.js`、`static/style.css`

这说明最基础的项目认知层已经不一致。

问题影响：

- 新人接手会先看错文件
- 后续 AI 或开发者辅助时容易改错位置
- 项目对外说明不可信
- 文档和代码脱节，维护成本持续上升

这类问题在顶级智能体项目里通常不会长期存在，因为它会直接拖垮后续迭代效率。

## 3.2 文档进度与真实实现不一致

`AGENTS.md` 和 `CLAUDE.md` 中写：

- 流式输出待完善

但当前主线后端 `backend/main.py` 实际已经做了 SSE 流式返回。

这说明文档没有随着代码演进更新。

问题影响：

- 团队对当前能力判断失真
- 容易重复开发已经完成的功能
- 无法准确识别真正的待办项

## 3.3 系统提示词过于轻量

`system_prompt.md` 当前只有三层内容：

- 角色
- 任务
- 规则

它适合原型阶段，但对顶级智能体来说太薄了。

当前缺失的关键内容包括：

- 来源优先级定义
- 引用策略
- 不确定时的退让策略
- 多轮对话策略
- 图片与文本混合场景策略
- 实验指导的安全边界
- 文档不足时该如何追问
- 计算与推导时的检查机制
- 仿真解释时的格式规范

顶级智能体不是只靠一句“你要严谨”，而是靠清晰、可执行、可约束的行为规范。

## 3.4 `AGENTS.md` 和 `CLAUDE.md` 内容高度重复

这两个文件几乎是重复文档，没有承担不同职责。

这意味着项目目前还没有把以下几类文档分开：

- 对外项目说明
- 给 AI/代理看的运行规则
- 给开发者看的架构说明
- 给维护者看的操作说明

顶级项目通常会把“面向谁写的文档”区分清楚，否则信息会互相污染。

## 3.5 现状报告已经指出问题，但还缺对标框架

`PROJECT_ANALYSIS.md` 已经很好地分析了：

- 主线收口
- 前端拆分
- RAG 升级
- 持久化
- 测试和日志

但它更偏工程整理视角，还没有完全站在“顶级智能体 PK”视角下给出能力差距矩阵。

也就是说，它告诉你“该整理什么”，但还没有完全告诉你“为什么这些问题会让你打不过更强的 Agent”。

## 4. 与顶级智能体相比的核心差距

如果要和顶级智能体产品对标，差距主要不在 UI，而在系统能力层。

## 4.1 差距一：你现在更像“带多模态的教学助手”，不是“真正的 Agent”

当前系统擅长：

- 接收问题
- 结合文档回答
- 结合图片解释
- 结合仿真参数生成讲解

但顶级智能体通常还具备：

- 自动规划问题解决步骤
- 根据任务动态决定使用什么工具
- 进行中间检查
- 做失败恢复
- 在多轮任务中保持目标一致性

你现在更像“强交互教学问答系统”，而不是“能独立完成复杂任务的 Agent”。

## 4.2 差距二：缺少结构化工具调用层

目前仿真联动方式主要是：

- 前端收集参数
- 拼自然语言 prompt
- 发给模型解释

这在原型阶段是对的，但顶级智能体更强调结构化工具能力，例如：

- 统一 schema
- 明确字段含义
- 工具结果可验证
- 模型和工具边界清晰

现在的纯 prompt 方式有几个弱点：

- 一旦 prompt 改写，结果稳定性波动大
- 很难做自动评估
- 很难做复杂工具链扩展

## 4.3 差距三：RAG 还不够强

你现在的 RAG 更接近：

- “把资料塞给模型”

顶级智能体更接近：

- “先检索、再筛选、再引用、再回答”

当前问题：

- 没有切块
- 没有索引
- 没有召回质量控制
- 没有证据排序
- 没有稳定引用体系

如果资料量上来，顶级智能体还能保持可解释性和命中率，而你当前方案会更快退化。

## 4.4 差距四：没有长期记忆与用户状态

顶级智能体往往不是只记当前会话，而是能逐步积累：

- 用户偏好
- 历史问题
- 学习进度
- 错误模式
- 长期任务上下文

你现在主要还是：

- 服务端内存 session
- 浏览器 localStorage 历史

这意味着系统缺少真正的“长期教学能力”。

## 4.5 差距五：缺少评测体系

顶级智能体项目通常都会逐步建立：

- prompt 评测集
- 回答质量基准
- 错误案例集
- 回归测试
- 模型切换对比集

你现在文档里提到测试和工程强化，但还没有形成“智能体质量评估框架”。

没有评测，后续所有优化都容易变成主观感觉。

## 4.6 差距六：提示词还不够工程化

当前 `system_prompt.md` 是一个“人能看懂”的提示词，但还不是一个“产品级、可迭代、可评测”的提示词系统。

顶级智能体的提示词通常会有这些特点：

- 有明确层级
- 有失败处理规则
- 有输入分流规则
- 有引用规则
- 有工具使用规则
- 有输出契约
- 有禁行区

你现在的提示词还偏“老师风格设定”，不够“系统行为规范”。

## 4.7 差距七：工程结构还不支持快速放大

即使功能已经有了，当前结构也不适合快速放大：

- `static/app.js` 太大
- 旧实现未彻底下线
- 文档不统一
- 配置未完全收束
- 仿真、实验、prompt 逻辑耦合

顶级项目的强点之一不是“多会说”，而是“迭代速度快且不容易坏”。

## 5. 当前最需要优化的内容

下面这部分按重要性排序，是最建议你优先修改的内容。

## 5.1 P0：统一项目主线和文档

必须先做：

- 更新 `AGENTS.md`
- 更新 `CLAUDE.md`
- 明确 `backend/main.py` 是唯一后端主入口
- 明确 `static/index.html` 是唯一页面入口
- 明确 `static/app.js` 和 `static/style.css` 是当前前端主资源
- 标出旧实现为 deprecated 或移除

为什么这是 P0：

因为如果文档还是假的，后面所有优化都容易跑偏。

## 5.2 P0：重写系统提示词

`system_prompt.md` 必须升级。

建议新增的提示词模块：

- 角色边界
- 证据优先级
- 文档引用规范
- 图片分析规范
- 物理推导规范
- 不确定时的追问策略
- 实验安全提示
- 多轮对话上下文策略
- 仿真场景解释格式
- 严禁编造规则

目标不是让提示词更长，而是让它更可执行。

## 5.3 P1：把实验仿真改造成结构化工具层

建议目标：

- 不再只是“拼一段自然语言”
- 而是先生成结构化实验状态对象
- 再基于该对象形成可控 prompt 或工具调用

例如应逐步形成：

- 实验 ID
- 当前参数
- 最近动作
- 观察到的现象
- 需要解释的目标

这样系统会更像真正的 Agent，而不是一层 prompt 包装。

## 5.4 P1：升级 RAG 到可检索形态

最低要求：

- 文档切块
- Chunk 元数据
- Query 召回
- Top-K 拼接
- 回答引用来源

最好进一步做到：

- 区分教材、实验讲义、用户上传报告
- 引入来源权重
- 文档命中为空时明确说明证据不足

## 5.5 P1：补持久化记忆

建议先上 SQLite：

- 保存 session
- 保存消息
- 保存文档
- 保存实验操作记录
- 保存用户历史提问摘要

后续如果你要和顶级智能体比“长期陪伴式学习”，这一步几乎逃不掉。

## 5.6 P1：建立评测集

这是最容易被忽略、但最值钱的部分。

建议你建立一套最小评测集，覆盖：

- 纯文本物理题
- 图片实验图分析
- 基于教材的问答
- 基于实验仿真的现象解释
- 故意给不完整信息的问题
- 诱导幻觉的问题

评测维度至少包括：

- 正确性
- 引用准确率
- 是否编造
- 解释清晰度
- 教学启发性

没有这套东西，就很难和顶级智能体做真正有意义的比较。

## 5.7 P2：拆前端和后端结构

建议拆分：

- 前端聊天层
- 前端仿真层
- 实验配置层
- prompt 模板层
- 后端模型层
- 后端上传/RAG 层
- 后端 session/persistence 层

这一步是为了后续快速迭代，而不是为了代码好看。

## 5.8 P2：增加日志、观测和失败恢复

至少要补：

- 模型调用日志
- API 异常日志
- 上传失败日志
- 文档解析失败日志
- 会话 ID 跟踪
- 模型超时处理
- 视觉模型失败回退策略

顶级智能体产品强的一个关键原因，就是故障时也能看清楚到底哪里出了问题。

## 6. 你最该修改的 MD 文件清单

如果你想从文档层先动手，建议按下面顺序改。

## 6.1 `AGENTS.md`

建议改成：

- 当前真实架构说明
- 唯一启动方式
- 当前能力说明
- 当前限制说明
- 后续路线图

不要再写旧入口、旧资源路径。

## 6.2 `CLAUDE.md`

如果保留，建议把它变成“面向 AI 代理的项目操作说明”，而不是复制 `AGENTS.md`。

建议内容：

- 主线文件位置
- 哪些文件是旧实现不要误改
- 哪些模块后续要拆
- 修改时的约束

这样它才有存在价值。

## 6.3 `system_prompt.md`

这是最值得大改的文件。

建议从“角色描述”升级为“行为协议”。

重点补：

- 何时必须基于资料回答
- 何时必须说明不确定
- 何时应该追问
- 何时应该分步骤解释
- 如何引用来源
- 如何处理实验图和题目截图
- 输出格式是否固定

## 6.4 `PROJECT_ANALYSIS.md`

这份文件已经不错，建议保留，但可以追加两块：

- 与顶级智能体的能力差距
- 智能体专项优化路线图

## 7. 建议的对标路线

如果你的目标真的是“和顶级智能体相 PK”，建议不要同时在所有方向发力，而是选一条你最有胜算的路线：

最适合你的路线不是“通用智能体”，而是：

- 物理实验教学垂直智能体

这条路的优势是：

- 场景清晰
- 数据可积累
- 仿真是你的独特武器
- 顶级通用智能体未必在这个垂类里天然占优

建议对标维度：

1. 教学解释质量
2. 实验现象分析能力
3. 图片题/实验图理解能力
4. 引用教材资料的可信度
5. 长期学习陪伴能力

不要一开始就去和通用 Agent 在所有任务上比，那不是你的优势区。

## 8. 最终结论

综合你当前的 MD 内容可以下一个很明确的判断：

你的项目已经有了产品雏形、场景壁垒和智能体特色，但目前最大的短板不是“功能太少”，而是：

- 文档主线不统一
- 提示词太轻
- RAG 太浅
- 记忆太弱
- 工具层不够结构化
- 缺少评测体系

如果只做 demo，它已经够用了。

如果目标是和顶级智能体相 PK，你后续必须优先补齐的是：

1. 文档与架构统一
2. 提示词工程化
3. 结构化工具层
4. 检索增强升级
5. 长期记忆
6. 评测体系

这六项补起来之后，你的项目才会从“做出了一个教学 Agent”进入“具备竞争力的垂直智能体系统”。
