# Physics Agent Project 项目现状报告

## 1. 基准结论

本报告以以下两处作为当前项目主线：

- 前端入口页面：`static/index.html`
- 后端主文件：`backend/main.py`

基于这条主线判断，项目当前已经完成了一个可运行的多模态物理实验教学智能体原型，重点能力不是“普通聊天”，而是把文档问答、图片理解、实验仿真和教学解释整合到了同一个页面流程里。

项目现在处于：

- 产品层面：主路径已跑通
- 技术层面：原型已成熟
- 工程层面：需要收口和整理

一句话概括：

这是一个已经能演示、能使用、能体现智能体特色的教学 Agent 原型，但还没有完成工程化定型。

## 2. 当前真实架构

### 2.1 前端主线

当前前端真实入口是 `static/index.html`，页面内直接挂载：

- `static/style.css`
- `static/app.js`

因此目前真正生效的前端逻辑不是 `static/js/app.js` 那套旧文件，而是根目录 `static/` 下的新版本资源。

当前前端承担了以下职责：

- 聊天界面渲染
- 文件上传入口
- 历史记录展示
- 实验库菜单展示
- 仿真窗口展示
- 仿真参数变化
- 将仿真状态转成提问 prompt
- 对接 SSE 流式输出
- Markdown + LaTeX 渲染

这说明前端不是一个简单壳子，而是项目的重要业务层。

### 2.2 后端主线

当前后端主文件应视为 `backend/main.py`。

这份文件已经承担了项目核心后端能力：

- FastAPI 服务入口
- DashScope 模型接入
- 文本与视觉模型路由
- 文档上传解析
- 图片上传与压缩
- 会话内存状态管理
- RAG 上下文拼接
- SSE 流式返回
- 静态资源挂载

虽然仓库里仍有 `app.py`，但从当前前端资源和接口返回结构看，`backend/main.py` 更应被视为主实现。

### 2.3 当前项目结构判断

当前项目不是“前后端分离部署”的复杂结构，而是一个单体式 Web Agent：

- 前端静态页面由 FastAPI 提供
- 交互逻辑在浏览器执行
- 模型调用在服务端执行
- 会话状态主要保存在服务端内存
- 历史输入补充保存在浏览器本地

这个结构对 demo、课程展示、实验教学助手原型来说是合理的，开发成本低，联调简单。

## 3. 当前已经完成的能力

## 3.1 聊天主链路

当前已经具备完整问答链路：

- 用户在 `static/index.html` 输入问题
- 前端通过 `fetch("/api/chat")` 发起请求
- 后端在 `backend/main.py` 接收请求
- 按是否包含图片决定调用文本模型或视觉模型
- 通过 SSE 方式实时返回内容
- 前端增量渲染消息

这说明项目已经不是一次性请求-响应，而是具备实时交互体验。

## 3.2 文档知识库链路

当前已经完成：

- PDF 解析
- DOCX 解析
- 上传文档后加入当前 session
- 将文档内容拼接为上下文注入模型

这使得智能体具备了“带用户私有资料作答”的能力。

但当前仍然属于轻量 RAG，核心机制是：

- 文本抽取
- 拼接
- 截断
- 注入模型

还没有做到：

- 文档切块
- 嵌入索引
- 召回排序
- 精细引用控制

所以它现在更像“知识注入”，还不算真正成熟的检索增强系统。

## 3.3 图片理解链路

当前已经完成：

- 图片上传
- 服务端图片压缩
- 调用视觉模型分析图片
- 与文本模式分开路由

这个设计是正确的，因为图像理解和文档注入共用上下文会快速消耗 token。现在的实现已经体现出一定的多模态设计意识，而不是简单把图片也塞进文本请求。

## 3.4 实验库与仿真联动

这是当前项目最有特色的部分。

实验库不是静态目录，而是一个可操作的教学入口。当前已具备：

- 实验列表展示
- 实验原理快捷提问
- 经典题目快捷提问
- 部分实验的交互式仿真
- 将仿真参数转成 prompt 再交给 Agent 分析

这意味着系统已经初步具备“环境状态 -> 语言解释”的链路，这是教学智能体区别于普通问答机器人的关键点。

## 3.5 前端体验层

当前前端体验已经比较完整：

- 单页应用布局
- 历史记录
- 毛玻璃 UI
- 拖拽上传
- 模型状态显示
- 消息流式展示
- Markdown 渲染
- LaTeX 渲染
- 仿真弹窗

这说明项目已经完成了“用户可感知价值”的那一层，不只是后台逻辑 demo。

## 4. 当前项目的智能体属性

如果从“智能体”视角来评价，这个项目已经具备以下要素：

- 有固定角色与任务边界
- 有领域提示词
- 有外部知识输入
- 有多模态输入
- 有仿真环境输入
- 有一定的教学输出格式约束

但它目前还不属于复杂 Agent 系统，因为还缺少这些能力：

- 规划与分解
- 多工具编排
- 显式观察-思考-行动循环
- 长期记忆
- 可验证执行链
- 失败恢复策略

因此当前更准确的定位是：

一个具备多模态输入、知识注入和仿真联动能力的教学智能体原型。

而不是：

一个已经完整工程化的通用 Agent 平台。

## 5. 当前最核心的问题

## 5.1 主线已确定，但仓库仍未彻底收口

既然你已经明确：

- 入口页面是 `static/index.html`
- 主文件是 `backend/main.py`

那仓库后续就应该围绕这条线收口。

当前问题是：

- `app.py` 仍然存在
- `static/js/app.js` 和 `static/css/style.css` 仍然存在
- 部分说明文件仍在描述旧结构

这会导致维护时出现认知成本：

- 哪个文件才是正式版本
- 改哪个接口才是真正生效
- 哪些旧代码还能删

## 5.2 `static/app.js` 体量过大

当前主前端文件集中了太多职责：

- 页面状态
- 上传逻辑
- 聊天逻辑
- SSE 处理
- Markdown 渲染
- 历史记录
- 实验库数据
- 仿真配置
- 仿真绘制
- prompt 生成

这在原型阶段可以接受，但继续往下做会很难维护。

后续如果继续加实验、加交互、加教学策略，这个文件会继续膨胀。

## 5.3 后端状态管理仍偏原型

当前会话状态主要依赖：

- 服务端内存 `SESSIONS`
- 浏览器 localStorage 历史

这意味着：

- 服务重启后 session 会丢
- 文档上下文无法长期保留
- 历史和上传数据无法跨设备同步

对于演示可以接受，但如果要长期使用或给多名学生使用，这会成为明显短板。

## 5.4 RAG 能力仍然偏浅

当前实现能做到“带资料回答”，但还做不到“资料库级检索”。

现阶段适合：

- 上传一份教材节选
- 上传一份实验讲义
- 上传一份实验报告辅助问答

不适合：

- 大量资料累计
- 课程级知识库
- 多轮基于大文档的稳定检索

## 5.5 工程保障不足

目前仍缺少这些基础能力：

- 测试
- 统一配置管理
- 错误日志规范
- 上传限制策略
- 安全边界说明
- 部署文档

也就是说，系统能跑，但还不够稳。

## 6. 项目阶段判断

当前项目不是“还没做完”，而是“核心价值已经做完，剩下是整理和强化”。

更具体地说：

- 最难的产品拼装已经完成
- 核心使用体验已经成立
- 现在要做的不是盲目加功能，而是把结构定下来

这个阶段最容易犯的错是继续不停堆功能，导致：

- 代码越来越乱
- 主线越来越不清晰
- 后续难以维护

所以现在最正确的动作不是继续扩展范围，而是先把已有成果整理成稳定版本。

## 7. 接下来必须做的内容

这一部分按优先级排序，属于真正建议落地的待办清单。

## 7.1 第一优先级：统一代码主线

这是最先要做的事情。

目标是把仓库明确成下面这条结构：

- 前端入口：`static/index.html`
- 前端主逻辑：`static/app.js`
- 前端主样式：`static/style.css`
- 后端主入口：`backend/main.py`

需要做的事：

1. 明确 `backend/main.py` 为唯一后端入口。
2. 明确 `static/index.html` 为唯一前端入口页面。
3. 检查并下线旧版 `app.py`。
4. 检查并下线旧版 `static/js/app.js` 和 `static/css/style.css`。
5. 更新项目说明文件，让文档与真实代码一致。
6. 给出唯一启动命令，避免后续再混乱。

这一阶段的目标不是“加新功能”，而是“让别人一眼知道项目怎么跑、怎么改、改哪里”。

## 7.2 第二优先级：拆分前端大文件

当前 `static/app.js` 已经承担过多职责，后续必须拆分。

建议拆分方向：

- `chat.js`
- `upload.js`
- `history.js`
- `experiments.js`
- `simulations/`
- `render/markdown.js`
- `render/math.js`
- `api.js`

最少也应该把这几类内容先拆开：

- 聊天与 SSE
- 实验库配置
- 仿真绘制逻辑
- prompt 生成逻辑

这样后续新增实验时，不至于继续把所有内容堆在一个 100KB+ 文件里。

## 7.3 第三优先级：整理实验数据和提示词

当前实验内容、实验名称、实验 prompt 和仿真逻辑关系比较紧耦合。

建议将以下内容结构化：

- 实验列表
- 实验元数据
- 实验说明模板
- 实验题目模板
- 仿真状态到 prompt 的映射模板

这一步的好处是：

- 新增实验更快
- 内容和逻辑分离
- 更方便后续做配置化管理

如果后面想扩展到 20 个、30 个实验，这一步几乎是必须的。

## 7.4 第四优先级：升级 RAG

如果你们后面想让它真正成为“教学智能体”，RAG 必须升级。

建议的升级方向：

1. 文档切块
2. 为切块建立索引
3. 根据问题做召回
4. 将召回结果而不是整份文档注入模型
5. 输出中保留来源标识

现在的拼接式方案适合快速原型，但一旦资料规模增大，回答质量和 token 成本都会迅速恶化。

## 7.5 第五优先级：补会话持久化

建议至少先补一个 SQLite 版本，不必一开始就上 Redis 或复杂数据库。

至少要保存这些内容：

- 会话 ID
- 用户消息
- 模型回复
- 上传文档元数据
- 文档抽取文本
- 上传图片元数据
- 实验操作记录

这样能解决几个现实问题：

- 服务重启后不丢上下文
- 能查看历史教学对话
- 后续可做实验学习记录

## 7.6 第六优先级：补测试和错误处理

目前建议补三类测试：

1. API 基础测试
2. 上传流程测试
3. RAG 拼接和 prompt 生成测试

同时补这些错误处理：

- 文件大小限制
- 文件类型校验
- 空文档处理
- 视觉模型失败回退
- DashScope 接口异常提示
- API key 缺失提示

这部分不会直接增加“炫”的功能，但会明显提升系统稳定性。

## 7.7 第七优先级：补日志和运维信息

目前如果线上或演示中出现问题，定位能力可能还不够。

建议增加：

- 请求日志
- 模型调用日志
- 上传日志
- 错误日志
- 关键指标统计

最少应该知道：

- 哪个接口报错
- 哪个 session 报错
- 调的是哪个模型
- 上传了什么类型的文件

否则后续调试会越来越困难。

## 8. 中期建议

如果项目后续还要继续做大，可以进入第二阶段建设。

第二阶段建议方向如下。

## 8.1 从“教学助手”升级到“教学工作流”

当前系统更像问答工具，后续可以进一步扩展为完整教学流：

- 实验预习
- 实验中指导
- 实验后总结
- 报告批改
- 常错点追踪

这样项目会从“单次问答”升级成“实验教学系统”。

## 8.2 从“提示词驱动”升级到“工具驱动”

当前仿真与 Agent 的联动方式主要是：

- 前端整理参数
- 拼 prompt
- 发送给模型

后续可以升级为更结构化的方式：

- 明确工具输入 schema
- 明确实验状态对象
- 明确模型消费字段

这样会比纯自然语言 prompt 更稳定，也更利于后续扩展。

## 8.3 从“单机会话”升级到“长期学习记录”

如果面向真实教学使用，可以逐步引入：

- 学生维度历史
- 实验完成记录
- 错题记录
- 学习进度

这才是真正能把教学 Agent 做深的方向。

## 9. 最终判断

以 `static/index.html` 和 `backend/main.py` 为主线重新判断后，项目现状可以概括为：

- 不是半成品
- 不是只会聊天的壳
- 不是简单的模型调用 demo

它已经是一个较完整的物理实验教学智能体原型。

当前最需要做的不是继续证明“能不能做”，而是尽快完成三件事：

1. 统一主线
2. 拆分结构
3. 强化 RAG 与持久化

如果这三件事做好，项目就会从“完成度高的原型”进入“可长期维护的教学智能体系统”。

## 10. 建议的实际执行顺序

为了避免后续工作发散，建议严格按下面顺序推进：

1. 统一入口与文档
2. 删除或归档旧实现
3. 拆 `static/app.js`
4. 抽离实验配置和 prompt 模板
5. 引入 SQLite 持久化
6. 升级 RAG
7. 增加测试和日志
8. 再考虑继续扩实验数量或新增模态能力

这会是当前阶段最稳、成本最低、收益最高的路线。
